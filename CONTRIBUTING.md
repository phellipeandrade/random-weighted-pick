# Contributing

## Development Setup

1. Install dependencies: `npm install`
2. Run tests: `npm test`
3. Build the library: `npm run build`

Please make sure tests and build pass before submitting a pull request.
